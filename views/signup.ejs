<!DOCTYPE html>
<html lang="en" ng-app="dystopia">
	<head>
		<meta charset="UTF-8">
		<title>Signup</title>
		<link rel="stylesheet" href="/static/css/styles.css">
		
		<script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.4.8/angular.min.js"></script>
		<script src="https://cdnjs.cloudflare.com/ajax/libs/angular-ui-router/0.2.15/angular-ui-router.min.js"></script>
		<script src="/static/js/app.js"></script>
	</head>
	<body ng-controller="MainCtrl">
		
		<h1>whatup this is the intro/signup page</h1>

		<ul>
			<li ng-repeat="player in players" ng-click="count = count + 1" ng-init="count = 0">
				<button>
					{{player.name}}
				</button>
				<div ng-hide="count % 2 === 0">
					{{player.role}}
					{{count}}
				</div>
			</li>
		</ul>
		<form ng-submit="addPlayer()">
			<label>Name</label>
			<input type="text" ng-model="newPlayer.name"> 	<br>
			<label>Role</label>
			<input type="text" ng-model="newPlayer.role">	<br>
			<button type="submit">Add player</button>
		</form>
	</body>
</html>